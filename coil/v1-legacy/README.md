# COIL v1 (legacy)

This directory holds the v1 COIL SYNC PROTOCOL™ implementation files
imported from the now-archived `coil-system` repo.

**Status:** Frozen. No further development here. New work targets v2 —
see [`../SPEC.md`](../SPEC.md).

## What's in here

| File | Role |
|---|---|
| `COIL_PROTOCOL.md` | v1 specification (frozen) |
| `COIL_Server.js` | v1 reference server (Node) |
| `COIL_Uploader_v2.html` … `v4.html` | v1 client uploaders |
| `COIL_compress.js`, `COIL_encrypt.js`, `COIL_deltaCheck.js` | v1 protocol helpers |
| `COIL_taskQueue.js`, `COIL_uploadPool.js` | v1 client-side queueing |
| `coil_data.py`, `coil_unpack.py` | v1 python tooling |
| `COIL_protocol_spec.txt` | shorthand v1 spec |
| `COIL_ALPHA_BACKTEST_REPORT.json` | v1 backtest output |
| `COIL_DASHBOARD_CONFIG.json` | v1 dashboard config |
| `COIL_SYNC_PROTOCOL.json` | v1 machine-readable spec |
| `COIL_MASTER_CHIP.json` | v1 reference master chip (frozen payload) |

The `coil-system` repo on GitHub has been archived; this tree is the
canonical v1 home going forward.
